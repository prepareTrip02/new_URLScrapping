package com.trip.prepare.scrapper.controller;

import org.aspectj.lang.reflect.CatchClauseSignature;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.chrome.ChromeOptions;
import org.springframework.boot.CommandLineRunner;
import org.springframework.boot.SpringApplication;

import com.trip.prepare.scrapper.SeleniumWebScrappingApplication;

public class MainTest{

	/*
	 * public static void main(String s[]) {
	 * 
	 * System.out.println("hello selenium"); // Create ChromeOptions and disable
	 * image loading and CSS ChromeOptions chromeOptions = new ChromeOptions();
	 * chromeOptions.addArguments("--disable-images");
	 * chromeOptions.addArguments("--disable-css");
	 * 
	 * // Create a WebDriver instance with ChromeOptions WebDriver driver = new
	 * ChromeDriver(chromeOptions); driver.manage().window().maximize(); try {
	 * driver.get("https://www.google.com");
	 * 
	 * System.out.println(driver.getTitle()); //driver.close(); //driver.quit();
	 * System.out.println("End..."); }catch (Exception e) {
	 * System.out.println("Exception="+e); // TODO: handle exception }finally {
	 * System.out.println("finally"); //driver.quit(); } }
	 */
	
	
}
